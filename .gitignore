# Dependencies
node_modules/
# package-lock.json is needed for CI/CD
yarn.lock
pnpm-lock.yaml

# Build outputs
dist/
*.tsbuildinfo

# Testing
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Env files — glob every variant. A bare ".env" list lets ".env production"
# and ".env copy" through, which is how a live API token nearly got committed.
.env*
!.env.example

# Agent scratch
.claude/

# Docs build
docs/.vitepress/dist
docs/.vitepress/cache
